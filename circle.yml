test:
  override:
    - pwd
    - ls -l
    - rm -rf bin/
    - rm -rf lib/
    - mkdir build
    - cd build && cmake -DBUILD_SHARED=ON .. && make
general:
  artifacts:
    - "headers"
    - "bin"
    - "lib"